#include "TurboBinarySearchTree.h"

/*namespace TurboCollections {*/

    // Insert implementation
    /*template<typename T>
    void TurboBinarySearchTree<T>::Insert(int data)
    {
        Node *newNode(data);
        if (root==nullptr)
        {
            return root =newNode;
        }
   
    
        if (data < root->value)
        {
        
            Insert(root->left->value);
        
        }

        else new Node = Insert(root->right->value);
    }*/

    

    //the left child has index n*2+1 
    //the right child has index n*2+2
    //(inserts a new item into the tree)
  
    

/*template <typename T>
bool TurboCollections::TurboBinarySearchTree<T>::Search(T)
{
    //(returns true if found)
}

template <typename T>
bool TurboCollections::TurboBinarySearchTree<T>::Delete(T)
{
    //(returns true if found and deleted),
}*/


